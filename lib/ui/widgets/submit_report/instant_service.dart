import 'package:flutter/material.dart';

class InstantService extends StatelessWidget {
  const InstantService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        
      ],
    );
  }
}